<!DOCTYPE html>
<html data-controller='webtheme' lang="en">
  <head>
    <title><%= content_for(:title) || 'Documenter' %></title>
    <meta name="viewport" content="width=device-width, initial-scale=1, user-scalable=no, interactive-widget=resizes-content">
    <meta name="view-transition" content="same-origin">
    <meta name="turbo-cache-control" content="no-cache">

    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>


    <%= javascript_importmap_tags %>
  </head>

  <body class="<%= yield(:body_page_class) %>">
    <header>
      <% if content_for?(:navbar) %>
        <%= yield(:navbar) %>
      <% else %>
        <%= render 'layouts/nav'%>
      <% end %>
    </header>

    <%= render 'layouts/messages'%>

    <div id='toolbox'>
      <%= yield(:toolbar) %>
    </div>

    <main class="<%= yield(:main_classes) %>" style='<%= !flash.empty? ? 'margin-top:2em;' : '' %>'>
      <%= yield %>
    </main>
    
    <footer>
      <% if content_for?(:footer) %>
        <%= yield(:footer) %>
      <% else %>
        <%= render 'layouts/footer'%>
      <% end %>
    </footer>


    <%= render 'layouts/confirm_dialog' %>
    <%= render "imagedialog/image_dialog" %>

  </body>
</html>
